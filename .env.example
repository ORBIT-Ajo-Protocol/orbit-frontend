# No environment variables are required to run this app as-is — everything
# is currently simulated client-side (see README "Status"). This file is a
# placeholder for when the frontend is wired to the real backend API.

# The orbit-backend base URL. Not consumed anywhere yet — see src/config.ts.
VITE_API_BASE_URL=http://localhost:4000
